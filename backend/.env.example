# Database Configuration
# Modifica "postgres" con la TUA password di PostgreSQL!
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/stappa_db?schema=public"

# JWT Secret (CHANGE IN PRODUCTION!)
# Usa una stringa random e sicura in produzione (minimo 32 caratteri)
JWT_SECRET="stappa_super_secret_key_change_in_production_2025"

# Server Configuration
PORT=3000
NODE_ENV=development

# Upload Settings
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
